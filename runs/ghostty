# -----------------------------
# Ghostty (no apt yet – AppImage)
# -----------------------------
mkdir -p ~/.local/bin
wget -O ~/.local/bin/ghostty https://github.com/ghostty-org/ghostty/releases/latest/download/ghostty-linux-x86_64
chmod +x ~/.local/bin/ghostty
